import * as classNames from "./styles.css";

export const RefereningDiv = () => {
  return (
    <div className={classNames.parent}>
      <button className={classNames.child}>Color</button>
    </div>
  );
};
